<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gsu.httpscs.rogueproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".adapter.ListNormalAdapter" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".SharedPreferenceActivity" />
        <activity
            android:name=".AdvanceViewPagerActivity"
            android:label="@string/title_activity_advance_view_pager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GestureActivity" />
        <activity android:name=".SubRotSingleTargetActivity" />
        <activity android:name=".SubRotMultipeTargetActivity" />
        <activity android:name=".SubRotDeathFromAboveActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".dialog.CustomDialog2" />
        <activity android:name=".AnimatorActivity"></activity>
    </application>

</manifest>